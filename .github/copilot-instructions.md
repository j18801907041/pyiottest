# Repository Instructions

- This repository is currently a minimal Python test project.
- Preserve the existing project structure and keep changes focused.
- Before running project-specific checks, inspect the repository for its declared tooling and test commands; none are currently present.
- Do not introduce dependencies or generated files unless the task requires them.